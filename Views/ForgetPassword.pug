extends Base
block content
     main.main
          .login-form
               h2.heading-secondary.ma-bt-lg Reset Your Passowrd
               form.form#forgetPassword-form(novalidate)
                    .form__group
                         label.form__label(for='email') Email address
                         input#email.form__input(type='email', placeholder='you@example.com', required)
                    .form__group
                        button.btn.btn--green Send

block script 
     script(src="/js/forgetPassword.js")